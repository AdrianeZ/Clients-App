<h2>{{client.name}}</h2>

<table class="table--left">
    <tbody>
        <tr>
            <th>Email</th>
            <td><a href="mailto:{{client.email}}" target="_blank">{{client.email}}</a></td>
        </tr>
        <tr>
            <th>Name</th>
            <td>{{client.name}}</td>
        </tr>
        <tr>
            <th> Data następnego kontaktu</th>
            <td>{{client.nextContactAt}}</td>
        </tr>
        {{#if client.notes}}
        <tr>
            <th>Notatki</th>
            <td>{{client.notes}}</td>
        </tr>
        {{/if}}
    </tbody>
</table>

<form action="/clients/form/edit/{{client.id}}">
    <button type="submit">Edit Client</button>
</form>

{{>go-home}}